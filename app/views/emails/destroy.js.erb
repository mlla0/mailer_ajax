$(document).ready(function(){

var element = document.getElementById("<%= @email.id %>");
    element.parentNode.removeChild(element);

var elementLi = $("[data-id=<%= @email.id %>]");
	elementLi.remove();

})